{
  "name": "kingdom-ads-site",
  "version": "1.0.0",
  "private": true,
  "description": "Kingdom Ads — Strategic Acquisition Architects (static marketing site).",
  "scripts": {
    "build:css": "tailwindcss -i src/styles.css -o assets/styles.css --minify",
    "watch:css": "tailwindcss -i src/styles.css -o assets/styles.css --watch",
    "build": "npm run build:css",
    "dev": "npm run watch:css"
  },
  "devDependencies": {
    "@tailwindcss/container-queries": "^0.1.1",
    "@tailwindcss/forms": "^0.5.10",
    "tailwindcss": "^3.4.17"
  }
}
